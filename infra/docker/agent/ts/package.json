{
  "name": "localcode-agent-ts",
  "private": true,
  "type": "module",
  "description": "localcode's own typescript in the agent image: model access for scripts that want one without going through opencode, and the session client agent code reports through.",
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.41",
    "@msgpack/msgpack": "^3.1.2",
    "ai": "^7.0.86",
    "jsonc-parser": "^3.3.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@ai-sdk/provider": "^4.0.9",
    "@types/bun": "^1.4.0",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "test": "bun test src processes",
    "typecheck": "tsc --noEmit"
  }
}
