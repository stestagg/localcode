{{/* Positioned into Gitea's navbar by custom/header.tmpl. Caddy serves both
     applications from one origin, so the root is all it takes. */}}
<div class="localcode-bar">
	<a href="/">Localcode</a>
</div>
